# 3D Models

Place the `zardonictext.glb` file in this directory.

The 3D logo component will automatically load and display it with parallax scrolling effect.

If the GLB file is not present, a fallback 3D placeholder will be displayed instead.
