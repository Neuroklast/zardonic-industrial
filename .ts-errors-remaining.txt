# TypeScript Errors Remaining

**Status: NOT CLEAN** — Last checked: 2026-06-16

The Next.js App Router bootstrap for migration phases 1-6 is in place, but `npm run typecheck` does **not** currently pass.

## Current blocker summary

The remaining failures come from legacy, not-yet-migrated modules that still reference dependencies removed in the bootstrap step or rely on Vite-era assumptions:

- Missing legacy packages still referenced in code/tests/APIs:
  - `@upstash/redis`
  - `@upstash/ratelimit`
  - `@vercel/blob/client`
  - `otpauth`
  - `recharts`
  - `i18next`
  - `react-i18next`
  - `i18next-http-backend`
  - `i18next-browser-languagedetector`
  - `@tiptap/react`
  - `@tiptap/starter-kit`
  - `@tiptap/extension-link`
  - `@tiptap/extension-image`
  - `@tiptap/extension-placeholder`
- Follow-on TypeScript errors are largely caused by those unresolved legacy integrations and by Vite-era imports/extensions that still exist in untouched migration areas.

## Validation snapshot

- `npm run lint` ✅ passes
- `npm run typecheck` ❌ fails
- `npm run build` ❌ fails (`@vercel/blob/client`, `recharts` still imported by migrated runtime code)
- `npm run test` ❌ fails (legacy suite still expects removed packages / pre-migration behavior)

## Next migration work required

1. Replace legacy Blob upload hooks/APIs with the new R2 upload flow.
2. Replace or remove `recharts`-backed admin/security panels.
3. Migrate CMS rich-text editor off Tiptap or restore an equivalent editor package.
4. Replace Upstash/Vercel auth/storage/rate-limit modules with Supabase/R2 equivalents.
5. Reconcile the remaining Vite-era test and API assumptions before expecting green validation.
