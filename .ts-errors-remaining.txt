# TypeScript Errors Remaining

**Status: CLEAN** — Last checked: 2026-06-16

## Validation snapshot

- `npm run lint`      ✅ passes (0 errors, 0 warnings)
- `npm run typecheck` ✅ passes (0 errors — `baseUrl` deprecation note is informational only, not an error)
- `npm run build`     ✅ passes (0 errors, 0 warnings)
- `npm run test`      ✅ passes (1863 tests across 125 files)

## Migration status

The Next.js App Router bootstrap (phases 1–11) is complete:

- Next.js 16 + Turbopack replacing Vite
- Supabase Auth + role-protected `/admin/*` middleware
- Admin CRUD pages: bio, gigs, releases, gallery, partners, social
- Cloudflare R2 storage utilities (`lib/storage/`)
- Zod schemas (`lib/schemas/`)
- Services layer (`services/`)
- Database migration (`supabase/migrations/001_initial_schema.sql`)

## Remaining migration work (phase 13+)

The legacy `api/` directory (Vercel serverless functions using Upstash Redis) is still present
alongside the new Next.js app routes. The packages `@upstash/redis`, `@upstash/ratelimit`,
`@vercel/blob`, and `otpauth` are kept as **devDependencies** so existing tests continue to pass
during the migration window. Once each legacy endpoint is fully replaced by a Supabase-backed
Next.js route, the corresponding test should be updated and the legacy files removed.

Priority replacements:
1. Auth (`api/auth.ts`, `api/session.ts`) → Supabase Auth (middleware + server actions)
2. CMS content (`api/cms/`) → Supabase DB via services layer
3. Rate limiting (`api/_ratelimit.ts`) → Supabase / edge middleware
4. Analytics (`api/analytics.ts`) → Supabase insert
